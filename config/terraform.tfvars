# This file contains actual configuration values (not secrets) and should be committed to git

# Vault connection settings
vault_address = "http://127.0.0.1:8200"

# Note: vault_token should be provided via environment variable VAULT_TOKEN
# for security reasons, not stored in this file
